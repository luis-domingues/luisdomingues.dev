---
interface Props {
    tags: string[];
}

const { tags } = Astro.props;
---
<ul class="flex flex-wrap gap-2 text-sm lg:text-base mx-8">
    {
        tags.map((tag) => {
            return <li class="border-b border-zinc-900 p-1">{ tag }</li>
        })
    }
</ul>