---
const articles = [
    {
        title: "Algoritmo de busca | Como solucionei um problema de busca",
        slug: "search_algorithm",
        publishDate: "16 de Abril, 2025",
        description:
            "Uma análise detalhada sobre como otimizei um algoritmo de busca para lidar com milhões de registros.",
    },
    // Adicione mais artigos aqui se necessário
];
---

<div class="max-w-3xl mx-auto space-y-8">
    <div class="mb-8">
        <h1
            class="text-2xl md:text-3xl font-bold tracking-tighter mb-2 flex items-center"
        >
            <span class="text-gray-500 mr-2">&gt;</span>
            BLOG<span class="cursor"></span>
        </h1>
        <p class="text-gray-600 text-sm tracking-wider">
            &lt;ARTIGOS E PENSAMENTOS/&gt;
        </p>
    </div>

    <div class="y2k-window">
        <div
            class="bg-gray-200 px-3 py-1 border-b border-gray-400 flex items-center"
        >
            <div class="mr-2 flex">
                <div class="w-3 h-3 rounded-full bg-gray-400 mr-1"></div>
                <div class="w-3 h-3 rounded-full bg-gray-400 mr-1"></div>
                <div class="w-3 h-3 rounded-full bg-gray-400"></div>
            </div>
            <div class="text-xs text-center flex-1 font-bold tracking-wider">
                ARTICLES.txt
            </div>
        </div>

        <div class="p-6 bg-white">
            <div class="space-y-6">
                {
                    articles.map((article) => (
                        <article class="group border-b border-gray-200 pb-6 last:border-0">
                            <a
                                href={`/blog/${article.slug}`}
                                class="block space-y-3 hover:bg-gray-50 p-2 transition-colors duration-200"
                            >
                                <div class="flex items-start">
                                    <span class="text-gray-400 group-hover:text-gray-900 transition-colors mr-2 text-lg">
                                        &bull;
                                    </span>
                                    <h2 class="font-mono font-bold text-lg">
                                        {article.title}
                                    </h2>
                                </div>

                                <div class="ml-5">
                                    <p class="text-gray-500 text-xs mb-2 font-mono">
                                        <span class="blink">[</span>
                                        {article.publishDate}
                                        <span class="blink">]</span>
                                    </p>
                                    <p class="text-gray-700 leading-relaxed">
                                        {article.description}
                                    </p>
                                    <div class="mt-3 text-sm text-gray-500 flex items-center">
                                        <span class="mr-1">READ_MORE</span>
                                        <span class="text-lg font-light">
                                            &rarr;
                                        </span>
                                    </div>
                                </div>
                            </a>
                        </article>
                    ))
                }
            </div>
        </div>
    </div>

    <div class="text-center text-sm text-gray-500 pt-2">
        <div class="inline-flex items-center space-x-1">
            <span class="text-xs font-mono">&lt;</span>
            <span>PÁGINA 1 DE 1</span>
            <span class="text-xs font-mono">&gt;</span>
        </div>
    </div>
</div>
